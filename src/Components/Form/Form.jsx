import React from "react";
import Button from "../Boxes/Button";
import Input from "../Boxes/Input";

const Form = () => {
  return (
    <div>
      <Input name="email" />
      <Input name="email" />
      <Input name="email" />
      <Button  />
    </div>
  );
};

export default Form;
